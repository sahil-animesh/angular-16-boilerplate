export interface TABLE_HEADERS_TYPE {
    name: string,
    key: string,
    type: string
}

export interface RECORDS_TYPE {
    /***
     * Here you can declare the name and the type of the properties according to the data you are receiving from backend
     */
}
